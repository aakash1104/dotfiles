# Install this in ~/.oh-my-zsh/custom

alias work="cd ~/Desktop/work"

alias up="cd .."
alias ..="cd .."

alias rm="rm -i"

alias vi="vim"

alias ga="git add"
alias gc="git commit -m"
alias gcm="git checkout master"
alias gb="git branch"
alias gp="git push -u origin"
